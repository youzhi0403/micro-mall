<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youzhi.dao.UserDao">

    <select id="list" resultMap="com.youzhi.mapper.UserMapper.BaseResultMap">
        select u.*
        from
        user u
        where
        1 = 1
        <if test="queryParam.nickname != null">
            and u.nickname like %#{queryParam.nickname}%
        </if>
        <if test="queryParam.account != null">
            and u.account = #{queryParam.account}
        </if>
        <if test="queryParam.telephone != null">
            and u.telephone = #{queryParam.telephone}
        </if>
        <if test="queryParam.deleteStatus != null">
            and u.delete_status = #{queryParam.deleteStatus}
        </if>
        order by u.create_time desc
    </select>
</mapper>